server:
  port: 8084

spring:
  application:
    name: fraud-service

  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.add.type.headers: true
        spring.json.type.mapping: "fraud_token:com.payment.fraud.dto.FraudScore"
    consumer:
      group-id: fraud-service
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      properties:
        spring.deserializer.value.delegate.class: org.springframework.kafka.support.serializer.JsonDeserializer
        spring.json.trusted.packages: "*"
        spring.json.use.type.headers: true
        spring.json.type.mapping: "tx_token:com.payment.fraud.dto.TransactionEvent, fraud_token:com.payment.fraud.dto.FraudScore"


  data:
    redis:
      host: localhost
      port: 6379
fraud:
  velocity-window-seconds: 60
  velocity-threshold: 5
  high-amount-threshold-cents: 500
  unusual-hour-start: 0
  unusual-hour-end: 6

